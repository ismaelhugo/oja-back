# Database - Railway PostgreSQL
# O Railway fornece automaticamente estas variáveis quando você adiciona o PostgreSQL
DATABASE_URL=postgresql://postgres:password@localhost:5432/railway

# Environment
NODE_ENV=development

# Server Port (Railway usa a variável PORT automaticamente)
PORT=3000

# Opcionais - Railway PostgreSQL (fornecidas automaticamente)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=railway
